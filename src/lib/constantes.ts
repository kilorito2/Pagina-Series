/**
 * Constantes compartidas entre código de Edge (middleware) y de Node
 * (route handlers, server components). No importar Prisma ni nada
 * Node-only desde este archivo.
 */
export const COOKIE_PERFIL_ACTIVO = "perfil_activo";
