/** Tags de `unstable_cache`/`revalidateTag` compartidos entre las queries
 * de lectura (home, catálogo) y las rutas de admin que mutan contenido. */
export const TAG_SERIES = "series";
export const TAG_GENEROS = "generos";
